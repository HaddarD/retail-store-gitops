apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    app: cart
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: cart
